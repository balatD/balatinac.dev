---
interface Props {
  schema: Record<string, unknown>;
}
const { schema } = Astro.props;
---

<!-- is:inline is explicit: this is a JSON-LD data block, not a script to bundle. -->
<script is:inline type="application/ld+json" set:html={JSON.stringify(schema)} />
